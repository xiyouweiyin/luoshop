server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    client:
      register-with-eureka: false
      fetch-registry: false

spring:
  application:
    name: dharma-service

  client:
    register-with-eureka: false
    fetch-registry: false